# HARNESS_CHANGELOG.md — ハーネス改訂台帳(makura-atlas)

原則: **エージェントがミスをするたびに、そのミスが二度と起きないようハーネスを改良する。**
起票条件: 同一失敗コード累計 2 回(LL-10)、または severity S1(LL-12)。

---

## HC-M001

| 項目 | 内容 |
|---|---|
| 起票日 | 2026-08-24 |
| トリガー | `GEN-LOGIC` × 2(makura-atlas loop_002: ラベル無衝突回避 → 修正版も w/h 入れ忘れの NaN 比較で無効) |
| 診断 | 二件は同根で、可視化の「見た目の正しさ」を目視スクリーンショットだけで判定していた。一件目は衝突回避の欠如そのもの、二件目は修正が効いていないことに目視では気づけず、DOM 計測(getBBox 総当たりの重なり数)で初めて確定した。目視は差分に鈍い |
| 改訂 | 検査スクリプト `harness/labelcheck.js` を常設し、TEST_SPEC の実行規約に「SVG ラベルを持つビューを変更したら、スクリーンショット目視に加えて重なり数=0 の数値検査を通す」を追記 |
| 種別 | tooling + test_spec_template(プロジェクト局所。レジストリ還流は同種失敗が他プロジェクトでも出た時点で判断) |
| SCAFFOLD_VERSION | 変更なし(プロジェクト局所改訂) |
| 効果検証 | 以後のビュー変更ループで GEN-LOGIC(可視化系)の再発 0 件なら Closed |
| propagation | makura-atlas ✅(局所) |
| 状態 | Verifying |
