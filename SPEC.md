# SPEC.md — makura-atlas(枕草子アトラス)

<!-- scaffold template v1.12.0 から展開(2026-08-24)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

『枕草子』44 章段を「快‐不快(valence)× 心の動き(arousal)」の二軸平面に置いた散布図アプリを土台に、
三つの拡張 —(1)類聚章段を項目単位に解体するドリルダウン、(2)日記章段を史実年表に重ねて
「書かれなかったこと」を可視化する沈黙ビュー、(3)季節×時刻の言及を升目に集計する
あけぼのマトリクス — を一つのアプリに統合する。座標は編者の解釈であることを明示し、
データ(JSON)を書き換えれば図全体が追随する構造とする。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 章段データ: 『枕草子』から **44 章段**(仕様定数)を採録する。各章段は id・題・型(随想 zuiso / 類聚 ruiju / 日記 nikki)・快‐不快 v ∈ [-1,1]・心の動き a ∈ [-1,1]・原文冒頭(excerpt)・情緒語タグ(tags)を持つ。v/a は編者の解釈値であり、データ改訂のみで図全体が追随する | must |
| F-02 | 座標ビュー: 44 章段を v×a 平面(横=快‐不快、縦=心の動き)に散布する。型で色分けし、点の選択で詳細パネル(縦書き原文・タグ・型)を表示する | must |
| F-03 | 情緒語横断検索: 情緒語タグ(をかし・あはれ・にくし…)のチップで散布図を絞り込める | must |
| F-04 | 解体ビュー(案1): 類聚章段のうち**解体対象と宣言した章段**(items.json の declared 集合)を項目単位に分解する。各項目は原文・現代語訳・v/a を持ち、親章段の一点から項目群が散らばるさまを同じ二軸で見せる | must |
| F-05 | 沈黙ビュー(案2): 全日記章段に推定年(year)・年表示(year_label)・比定根拠(dating_basis)・確度(dating_conf: high/mid/low)を持たせ、横軸=史実の時間(990–1001)・縦軸=快‐不快で散布する。背景に中関白家の栄華と没落の史実年表(events.json: 年・事件・明暗 tone)を帯として敷き、史実の暗転後も章段の座標が快側に留まる乖離を読ませる | must |
| F-06 | 升目ビュー(案3): 章段の季節×時刻言及を koyomi タグ(season × time + 根拠語 evidence)として持たせ、季節(春夏秋冬+通季)× 時刻(暁・あけぼの・つとめて・昼・夕暮・夜)のグリッドに集計する。セル選択で該当章段と根拠語を表示する | must |
| F-07 | 縦書き: 原文は writing-mode: vertical-rl + 明朝体で組み、UI(操作系)はゴシック体とする。本文と操作系のレイヤを字面で分ける | must |
| F-08 | 下部固定フッタ: saijiki-lens の AppFooter(F-13)と同書式の帯を全画面下部に固定する。MIT License / © 2026 坂田哲朗 / GitHub / 歩き方アーティファクト / 設計図アーティファクト / App Menu へのリンクを含み、本文下端は帯に隠れない余白を取る | must |
| F-09 | 解説アーティファクト: 操作説明「枕草子アトラスの歩き方」と「枕草子アトラス設計図」の 2 本を公開し、フッタから参照する | must |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | ビルド不要の静的サイト(HTML+CSS+JS+JSON)。フレームワーク・バンドラを使わない | web/ 直下のファイル構成の目視 + T-011 |
| N-02 | データ主導: 座標・比定・koyomi はすべて JSON のフィールドで、コード変更なしに改訂できる。**段番号は持たせない**(三巻本・能因本・堺本で振り方が異なるため。三巻本準拠で振る場合は id の隣にフィールドを足す) | T-013 |
| N-03 | 派生値(升目集計・沈黙ビューの帯)は入力 JSON から決定論に導出する。UI 側に集計済み定数を埋め込まない | T-008 + コードレビュー |

## 4. 品質基準

- 章段数はちょうど 44(F-01 の仕様定数)。id は一意、v/a は [-1,1]、型は 3 値 enum
- 項目被覆の不変量: items.json が宣言する解体対象章段の集合 = 項目が実在する章段の集合(取りこぼしも余分も無い)。項目の chapId はすべて類聚章段を指す
- 日記章段は全件が year(990–1001)・year_label・dating_basis(非空)・dating_conf(enum)を持ち、非日記章段は year を持たない
- events.json は年昇順で、史実アンカーとして少なくとも「長徳の変(996)」と「定子崩御(1000)」を含む
- koyomi の season/time は語彙表(§F-06)に含まれ、evidence は非空。初段「春はあけぼの」は四季 4 タグを持つ(原文の構造そのものが出所)
- フッタ帯(.app-footer)が index.html に存在し、F-08 の必須リンクを全て含む

## 5. スコープ外

- 全 300 余段への拡張、および青空文庫 / J-TEXTS 底本からの自動パース(次期。データ生成経路だけ N-02 で確保)
- 情緒語の共起ネットワーク、季節分布の時系列分析
- 投稿・集計・共有などのサーバ機能(完全静的)
- 諸本(三巻本・能因本・堺本)の異同表示。底本間で安定な「題」だけを見出しに使う
- 座標値の学術的正当化。v/a は編者の解釈であることを UI 上に明示する(隠さない)
